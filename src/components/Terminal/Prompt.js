import React from "react";

const Prompt = ({currentDir}) => <span className="terminalText"> Retro Personal Website:\ {currentDir} <span style={{fontSize:"14px"}}>{">"}</span> </span>;

export default Prompt;